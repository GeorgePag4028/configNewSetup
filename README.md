# configNewSetup

This is a way to config a new setup 

install vim 
install git 
install chrome
install keygen
add keygen to github
map win+enter => terminal 
map win+g => chrome
copy .vimrc
install tmux 
copy .tmux
